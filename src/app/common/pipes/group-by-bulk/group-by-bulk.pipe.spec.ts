import { GroupByBulkPipe } from './group-by-bulk.pipe';

describe('GroupByBulkPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByBulkPipe();
    expect(pipe).toBeTruthy();
  });
});
