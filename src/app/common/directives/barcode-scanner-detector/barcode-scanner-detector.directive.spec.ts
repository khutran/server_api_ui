import { BarcodeScannerDetectorDirective } from './barcode-scanner-detector.directive';

describe('BarcodeScannerDetector', () => {
  it('should create an instance', () => {
    const directive = new BarcodeScannerDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
