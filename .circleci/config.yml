# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: reflaunt/circle-ci-nodejs:v0.1

    working_directory: ~/repo

    steps:
      - checkout
      # Download and cache dependencies
      - restore_cache:
          keys:
          - v2-dependencies-rf-admin-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v2-dependencies-rf-admin-

      - run: yarn install

      - run:
         name: Deploying
         command: |
           if [[ "$CIRCLE_BRANCH" == "dev" ]]; then
             yarn build_dev
           fi
           if [[ "$CIRCLE_BRANCH" == "production" ]]; then
             yarn build_prod
           fi

      - save_cache:
          paths:
            - node_modules
          key: v2-dependencies-rf-admin-{{ checksum "package.json" }}

      - run:
          name: Deploying
          command: |
            if [[ "$CIRCLE_BRANCH" == "dev" ]]; then
              aws s3 cp --region=ap-southeast-1 --recursive dist s3://admin-dev2.reflaunt.com
            fi

            if [[ "$CIRCLE_BRANCH" == "production" ]]; then
              aws s3 cp --region=ap-southeast-1 --recursive dist s3://admin.reflaunt.com
            fi
workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            branches:
              only:
                - dev
                - production
